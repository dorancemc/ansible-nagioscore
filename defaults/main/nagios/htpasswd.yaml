---
# users of the apache AuthUserFile that guards the nagios cgi, owned by this
# role because apache and its configuration are owned by this role
# state absent removes the user from the file
nagioscore_htpasswd_users:
  nagiosadmin:
    password: "{{ nagiosadmin_password }}"
    state: present
